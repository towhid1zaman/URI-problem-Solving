#include<stdio.h>
int main()
{
    int A,B,NUMBER;
    double C,SALARY;
    scanf("%d%d%lf", &A,&B,&C);
    SALARY = B*C;
    printf("NUMBER = %d\n", A);
    printf("SALARY = U$ %0.2lf\n", SALARY);

    return 0;
}
